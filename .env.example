# Application
APP_PORT=8080
APP_ENV=development

# Database
POSTGRES_DB=cedar
POSTGRES_USER=cedar
POSTGRES_PASSWORD=cedar
POSTGRES_PORT=5432
DATABASE_URL=postgres://cedar:cedar@localhost:5432/cedar?sslmode=disable

# Redis
REDIS_PORT=6379
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# Authorization caching (requires Redis). Set to 0s to disable.
AUTHZ_CACHE_TTL=5s

# CORS (comma-separated origins). Use '*' for local dev.
CORS_ALLOW_ORIGINS=*

# Frontend
WEB_PORT=5173
VITE_API_BASE_URL=http://localhost:8080
